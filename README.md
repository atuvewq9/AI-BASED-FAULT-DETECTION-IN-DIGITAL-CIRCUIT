# AI-BASED-FAULT-DETECTION-IN-DIGITAL-CIRCUIT
The core idea — digital circuits can have manufacturing defects or runtime failures. The most common model is "stuck-at faults" (a signal stuck at 0 or 1 regardless of input). Your AI model learns to detect these by analyzing the difference between expected and actual circuit outputs. 
